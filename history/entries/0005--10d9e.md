# Entry 0005 — SCORE 10722584 — n at the security floor

| Field | Value |
|-------|-------|
| Date | 2026-06-29 |
| Author | @10d9e |
| Model | opus 4.8 |
| Commit | `nfloor` |
| SCORE | 10722584 |
| Status | record (boolean challenge) |

## Change

Drop the blind-rotation length n from 700 to **680** — the LWE security floor at σ=2^48
(130-bit; n=672 → 128.6 is the hard edge). σ is unchanged so the noise margin holds (4.x bits);
fewer CMux ⇒ ~3% faster. Best-of-5 A/B: n=672 ~9.78 ms vs n=700 ~10.18 ms.

## Negative results recorded this round (boolean, k=4)

- **ℓ=2 decomposition**: margin tops out at 3.1 (baselog=10) — the decomp-error vs bsk-noise
  (B/2) tension; ℓ=3 stays the floor.
- **D=768 (k=3, N=256)**: fails the *security* gate — the GLWE dimension can't drop below 1024
  at any usable σ (noise would explode), so D=1024 is the FFT floor.
- **f32 bootstrap key**: no speedup (the pointwise MAC is compute-bound, not memory-bound — the
  f32→f64 convert offsets the halved memory), and it cost ~1 bit of margin. Reverted.

These pin k=4 / N=256 / ℓ=3 / D=1024 as the structural optimum for this scheme; the FFT∝(k+1)/(2k)
and MAC∝(k+1)² terms balance at k=4.

## Eval snapshot

```
params: n=680 k=4 N=256 | LWE σ=2^48.0 dim 680 → 130.1 bits | GLWE σ=2^39.0 dim 1024 → 129.6 bits
security gate: OK (≥128 bits, standard model)
fixture           got want  ok
identity/m0         0    0  OK
increment/m0        1    1  OK
identity/m1         1    1  OK
increment/m1        0    0  OK
--------------------------------------------
output noise: σ=2^56.9, failure margin 4.1 bits (need ≥3.5)

SCORE: 10722584 ns/bootstrap  (median of 21; best 10643709 ns) — LOWER IS BETTER
       = 10.723 ms
```
