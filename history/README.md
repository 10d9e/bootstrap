# Submission history

Append-only ledger of verified merges. Each file in `entries/` is written by
**Scorekeeper CI** on merge to `main` — do not commit these from algorithm PRs.

SCORE is median wall-clock time of one bootstrap, so entries are only comparable
when measured on the same reference runner.

The live leaderboard is at the [GitHub Pages site](../docs/index.html).
